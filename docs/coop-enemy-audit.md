# Co-op Enemy Audit

This audit tracks enemy and enemy-like actor files that may need co-op-aware player targeting. It is separate from `docs/coop-player-owner-lookup-audit.md`: item actors usually need owner lookup, while enemies need target selection policy.

## Targeting Architecture

The intended layering is:

```text
actor patches -> enemy_targeting -> player_query
```

- `player_query`: raw facts about active players: candidates, distances, angles, slots, and diagnostics.
- `enemy_targeting`: policy for choosing and retaining a target through reusable behavior scopes.
- actor patches: narrow conversions at concrete enemy callsites, preserving vanilla behavior outside the scoped PC/co-op hook.

The current Bokoblin, Tektite, Stalhound, Stalchild, Gibdo, and Chilfos proofs now use
`enemy_targeting` over `player_query`. Bokoblin remains the richer melee validation surface;
Tektite is the first compact non-Bokoblin port, Stalhound and Stalchild are breadth proofs, Gibdo is
the first target-state-sensitive humanoid/undead proof, and Chilfos is the first melee-plus-thrown
weapon stress test. Choose the next enemy from the audit queue rather than widening any one proof
surface by default.

## Target Policy Requirements

Nearest-player selection is a useful primitive, but it is too twitchy as a universal enemy policy. The policy layer should support at least:

- target acquisition from nearest/visible active player candidates,
- sticky target retention for simulation seconds, not render frames,
- attack follow-through that does not retarget mid-attack unless the target disappears or becomes invalid,
- recent attacker bias so damage can pull attention,
- optional target-pressure weighting so one player does not receive every enemy in a crowd,
- diagnostics that show why a target was selected, retained, or changed.

Do not build all of this before the audit is useful. Start with sticky retention and attack follow-through, then add bias/pressure only when a tested enemy needs it.

Every converted enemy must use an actor-local helper near the top of the enemy file. The helper builds `EnemyTargetContext`, sets a reusable behavior scope such as `EnemyTargetScope::Combat`, and accepts a manual diagnostic label from the original callsite. The scope owns target state; labels explain which original callsite consumed that state. Do not key retention by labels or create independent per-callsite retention machines.

## Conversion Checklist

For each enemy family, classify and test these layers separately:

| Layer | What To Look For | Status Values |
| --- | --- | --- |
| Search/wake | Idle recognition, proximity checks, line-of-sight checks | P1-only / raw-query / policy-backed / validated |
| Chase/turn | Movement target position, face-player angle, home-distance checks | P1-only / raw-query / policy-backed / validated |
| Attack gate | Distance and angle checks that decide whether an attack starts | P1-only / raw-query / policy-backed / validated |
| Follow-through | Attack animation/facing after startup | P1-only / raw-query / policy-backed / validated |
| Damage/guard/cut | Hit reactions, shield checks, cut-type checks, special player states | P1-only / owner-aware / global-by-design / needs audit |
| Projectile/grab/spawn | Spawned enemy weapons, grabs, catches, thrown objects | P1-only / owner-aware / host-owned / needs audit |
| Item awareness | Hookshot, boomerang, bomb, bait, tool, or owned-item reactions | P1-only / active-player scan / owner-aware / deferred |
| Spawn/presentation | Master/child spawning, child facing, intro/fanfare angles, camera-facing presentation | P1-only / target-slot-aware / global-by-design / deferred |
| Demo/event/camera | Special cameras, scripted scenes, boss/event behavior | global-by-design / deferred / needs audit |

Before moving from one enemy to the next, update that enemy's row with every left-out or deferred API
hook discovered during implementation. Do not rely on memory or on "mostly works in-game." Name the
missing family explicitly: damage-owner, defender-owner, selected-target state, item awareness,
master/child ownership, presentation/camera ownership, caught/grab/swallow/hang ownership, broader
collision-owner, render/visibility culling, or story/demo/global state.

## Current Proofs

| Actor | Name | File | Profile | Current State | Notes |
| --- | --- | --- | --- | --- | --- |
| Hanging Helmasaur | Hanging Helmasaur | `src/d/actor/d_a_e_hm.cpp` | `E_HM` | raw-query proof | Only `e_hm.up_wait` wake/proximity is converted. Other combat and damage behavior remains P1/global. |
| Basic Bokoblin | Bokoblin | `src/d/actor/d_a_e_oc.cpp` | `E_OC` | policy-backed targeting, owner APIs validated | Search, head-search, find/chase, move-out, attack gates, and follow-through use `enemy_targeting`; chase obstacle steering and sword-sound awareness use `selected_target_state`; group battle participation uses active-player `player_query`; sword hit reactions use `damage_owner`; guard collision uses `defender_owner`. |
| Tektite | Tektite | `src/d/actor/d_a_e_tt.cpp` | `E_TT` | policy-backed targeting, owner APIs validated | Search/chase/attack/out-range use `enemy_targeting`; ordinary target facts and first-attack prediction use `selected_target_state`; cut reactions use `damage_owner`. Culling remains render/visibility work. |
| Stalhound | Stalhound | `src/d/actor/d_a_e_sh.cpp` | `E_SH` | policy-backed targeting, first-pass validated | Central target metrics, movement speed, attack commitment, head tracking, and damage knockback angle route through the co-op API families. No P1 guard-state read was found; attack shield response currently uses the collision `ChkAtShieldHit()` flag. First surface test looked good. |
| Stalchild | Stalchild | `src/d/actor/d_a_e_bs.cpp` | `E_BS` | policy-backed targeting, first-pass validated | Swarm-style ground melee proof. Recognition, chase/attack target metrics, attack-facing checks, head tracking, and attack guard response route through the co-op API families. |
| Gibdo | Gibdo | `src/d/actor/d_a_e_gi.cpp` | `E_GI` | policy-backed targeting, owner APIs validated | Sleep/wait/chase/attack/damage recovery and head tracking route through `enemy_targeting` plus `selected_target_state`; the close-range attack/scream gate uses immediate acquisition so stale chase retention cannot suppress an in-range player; ordinary sword cut reactions use `damage_owner`; scream stun uses `caught_stun_owner` so the retained slot owns release input and best-effort camera lock while nearby affected slots share the scream animation timer with an expanded co-op AoE. Wolf-bite ownership remains intentionally deferred to a later caught/grab-owner proof enemy. `gibdo.state` remains available for follow-up debugging and records native range/angle/LOS/delay/scream-owner gates. Some placed Gibdos use `mSwbit2` switch gating before sight checks, so apparent P1-only activation may also involve room script state. Simultaneous per-player scream ownership is documented as deferred because vanilla `m_cry_gi` also coordinates follow-up attacks. |
| Young Gohma | Young Gohma | `src/d/actor/d_a_e_kg.cpp` | `E_KG` | policy-backed targeting, first-pass validated | Central action target metrics route through `enemy_targeting` plus `selected_target_state`; move/search and attack gates share the selected target's distance, angle, and line-of-sight actor. Roof/drop front-roll awareness uses the selected player's state instead of P1. Damage remains routed through the shared `cc_at_check()` owner path. `young_gohma.state` records range/cone/LOS and `pl_check` gate facts. The post-attack wander gap was confirmed to match vanilla Young Gohma behavior rather than co-op target loss. |
| White Wolfos | White Wolfos | `src/d/actor/d_a_e_ww.cpp` | `E_WW` | policy-backed targeting, first-pass validated | Combat chase/attack/walk/move-out paths use one Combat owner plus `selected_target_state`; guard contact uses `defender_owner`; hookshot side-step awareness scans active players for live hookshot top positions instead of asking P1; master spawn staging can wake on P2; child facing follows the encounter anchor; presentation angles use the selected slot's camera when available. Remaining special/demo presentation and any broader spawn ownership should be documented as presentation/camera or master/child ownership if encountered in later passes. |
| Chilfos | Chilfos / Ice Swordsman | `src/d/actor/d_a_e_kk.cpp` | `E_KK` | policy-backed targeting, first-pass validated | Wait/wake, action selection, spear throw, attack, guard, backwalk, damage recovery, and head tracking route through one Combat owner plus `selected_target_state`. Damage/cut reactions and iron-ball owner status use `damage_owner`; shield posture uses the selected combat target's cut facts; thrown spear child launch math inherits the parent Combat target when available. P2 testing and diagnostics showed `spear_throw`, `weapon_move`, `next_action`, `damage_shield`, and `head_track` following slot 1. No defender-owner, caught/grab, or item-awareness surfaces were found in this pass. |
| Armos | Armos | `src/d/actor/d_a_e_ai.cpp` | `E_AI` | policy-backed targeting, first-pass validated | Wake, movement, attack gates, line-of-sight checks, and shield-facing state route through one Combat owner plus `selected_target_state`; sword cut reactions use `damage_owner` without falling back to P1 when ownership is unresolved. Field testing found P2 behavior matched expectations. No defender-owner or caught/grab/spawn surfaces were found in this first pass. |
| Mini Freezard | Mini Freezard | `src/d/actor/d_a_e_fz.cpp` | `E_FZ` | policy-backed targeting, pending validation | Wait/move/attack gates, attention LOS, rebound angles, and damage bounce steering route through one Combat owner plus `selected_target_state`; attack contact uses `defender_owner`. True item-owner/item-awareness bias for boomerang, hookshot, spinner, and arrow reactions remains deferred; Blizzeta roll-mode is boss-owned/special and left vanilla. |
| Big Freezard | Big Freezard | `src/d/actor/d_a_e_fb.cpp` | `E_FB` | policy-backed static/ranged targeting, pending validation | Detection, LOS, head pitch, vertical eligibility, selected-target heavy-boots/status0 gates, caged/sweep side gates, Mini Freezard spawn facing, and bullet hit counting route through one Combat owner plus `selected_target_state` / active-player identity. Direct iron-ball hit counting now treats any active player like vanilla P1; cannon-carry instant kill remains native object behavior. |
| Donketsu | Donketsu | `src/d/actor/d_a_e_mm.cpp` | `E_MM` | policy-backed targeting, pending validation | Search/dash metrics route through one Combat owner plus `selected_target_state`; attack shield reflection records `defender_owner`. The native `dComIfGp_checkPlayerStatus0(0, 0x100)` gate remains a deferred selected-target/player-status hook until its exact meaning is classified; armor child spawning for `E_MM_MT` remains vanilla master/child ownership. |
| Bomb Bug | Bomb Bug | `src/d/actor/d_a_e_bi.cpp` | `E_BI` | policy-backed targeting, first-pass validated | Wake/search, tracking, close attack gate, room sightline, home-distance checks, and the `fopAcStts_UNK_0x80000_e` collision-status band route through one Combat owner plus `selected_target_state` / `player_camera_status`; stun knockback facing uses `damage_owner`. Hookshot/boomerang-created enemy bombs now inherit the hit owner before NBOMB create-time carry/boomerang setup runs, and shared boomerang movement retains that owner so P2 boomerang-carried bombs return to P2 instead of P1. Field testing found P2 behavior matched expectations. Carry-created bomb ownership and carry attention remain deferred collision/carry-owner surfaces. |
| Deku Baba | Deku Baba / Bokkuri Baba | `src/d/actor/d_a_e_hb.cpp` | `E_HB` | policy-backed targeting + bite ownership, first-pass validated | Wake/search and action-wide angle/full-distance caches route through one Combat owner plus `selected_target_state`; roof/vertical pitch uses the selected target's eye position. Shield/contact bounce uses `defender_owner`, cut/death reactions use `damage_owner`, and bite/caught lifetime uses `retained_interaction_owner` so the struck player stays retained through biting, pull, release, and cleanup. Leaf/Bokkuri helper spawning remains native self/helper behavior. |
| Giant Baba | Giant Baba / Big Deku Baba | `src/d/actor/d_a_e_gb.cpp` | `E_GB` | policy-backed targeting, first-pass validated | Head wait/attack thresholds, action-wide face pitch/yaw, and near-stop checks route through one Combat owner plus `selected_target_state`; active boomerang radius expansion uses `item_awareness`. Flower/body, bomb-eat, key/drop, boss-room wait, and demo camera/player-placement paths remain authored P1/global presentation or object behavior. |
| Armos Titan | Armos Titan / Idelia Statue | `src/d/actor/d_a_e_is.cpp` | `E_IS` | beta/deferred validation | Beta/unused variant skipped from the current field batch. Existing first-pass hooks route wake/move/attack search gates, LOS, attack range, and steering through one Combat owner plus `selected_target_state`; damage remains routed through shared `cc_at_check()`. Switch-gated trap/poweroff behavior remains vanilla/global room logic. |
| Bombskit | Bombskit / Crazy Runner | `src/d/actor/d_a_e_cr.cpp` | `E_CR` | policy-backed targeting, first-pass validated | Proximity awareness and evasive chase angles route through one Combat owner plus `selected_target_state`; damage remains routed through shared `cc_at_check()`. Field testing found P2 behavior matched expectations. Egg child spawning remains vanilla self-owned behavior; no defender/caught/grab/camera surfaces were found in this first pass. |
| Bari | Bari | `src/d/actor/d_a_e_dk.cpp` | `E_DK` | policy-backed targeting, first-pass validated | Search, chase hop direction, attack gates, first-attack cooldown distance, and selected-target electric contact state route through one Combat owner plus `selected_target_state`; damage remains routed through shared `cc_at_check()`. Field testing found P2 behavior matched expectations. Body/core split appears self-contained; no defender/caught/grab/spawn/camera surfaces were found in this first pass. |
| Dodongo | Dodongo | `src/d/actor/d_a_e_dd.cpp` | `E_DD` | policy-backed targeting, pending validation | Fire-breathing melee pass. The native dispatcher's cached player angle/distance now comes from one Combat owner for search, run, flame, tail reaction, and LOS gates; selected-target state supplies the close-range cut-active shield gate; jump-cancel tail reactions use `damage_owner` facts from the player who struck it. Bomb suction/search and bomb/arrow death counters remain native object/hit-type behavior. No defender-owner, caught/grab, spawned-child, or camera/demo surfaces were found in this pass. |
| Tadpole | Tadpole | `src/d/actor/d_a_e_tk.cpp` | `E_TK` | policy-backed targeting, pending validation | Wait/find/attack/path-swim awareness, vertical eligibility, LOS, facing, and movement metrics route through one Combat owner plus `selected_target_state`; damage remains routed through shared `cc_at_check()`. Projectile ball aim inherits the parent tadpole's Combat target, and sword reflection uses `damage_owner` so the player who struck the ball owns cut-state checks. The shared projectile file includes water and fire Toadpoli ball types. No defender/caught/grab/camera surfaces were found in this first pass. |
| Big Tadpole | Big Tadpole | `src/d/actor/d_a_e_tk2.cpp` | `E_TK2` | policy-backed targeting, pending validation | Wait/find/attack awareness, LOS, facing, and movement metrics route through one Combat owner plus `selected_target_state`; damage remains routed through shared `cc_at_check()`. Projectile ball aim inherits the parent tadpole's Combat target, and sword reflection uses `damage_owner` so the player who struck the ball owns cut-state checks. The shared projectile file includes water and fire Toadpoli ball types. No defender/caught/grab/camera surfaces were found in this first pass. |
| Chuchu 2 | Chuchu 2 / large splitting Chu | `src/d/actor/d_a_e_sm2.cpp` | `E_SM2` | policy-backed targeting, first-pass validated | Normal move awareness, action-wide target metrics, and roof-drop XZ proximity route through one Combat owner / active-player query while preserving the native distance dimension. Bottle-catch effect presentation uses `event_owner`. Field testing found P2 behavior matched expectations. Merge/split/water/fail behavior otherwise remains native self/state behavior; the camera visibility cleanup path remains vanilla camera/presentation logic. No damage-owner, defender-owner, caught/grab, or spawn surfaces were converted in this first pass. |
| Chu Worm / Slime | Chu Worm / Slime Chu | `src/d/actor/d_a_e_sm.cpp` | `E_SM` | policy-backed targeting + hook carry owner, first-pass validated | Wake/LOS and the action-wide XZ angle/distance cache route through one Combat owner plus `selected_target_state`, preserving native horizontal distance semantics. Body contact uses `defender_owner`; loose hookshot awareness scans active players; hookshot core hits use `damage_owner` and retain Carry ownership through `retained_interaction_owner` so release and carry-offset writes follow the player who hooked the core. Demo/camera presentation and special room intro paths remain P1/global authored presentation. |
| Walltula | Walltula | `src/d/actor/d_a_e_ws.cpp` | `E_WS` | policy-backed targeting, pending validation | Wall/climb awareness, attack continuation, and attack facing route through one Combat owner plus `selected_target_state`, using the selected player's climb/status facts instead of P1. Damage remains routed through shared `cc_at_check()`. No defender/caught/grab/spawn/camera surfaces were found in this first pass. |
| Keese | Keese / Fire Keese / Ice Keese | `src/d/actor/d_a_e_ba.cpp` | `E_BA` | policy-backed targeting, pending validation | One actor handles normal, fire, and ice Keese via resource/type variants: `E_ba`, `E_fb`, and `E_ib`. Wake/re-engage checks, hover/orbit positions, dive attack startup, and battle attention height checks route through one Combat owner plus `selected_target_state`. Wolf-bite hold starts from `damage_owner`, then uses `wolf_catch_owner` for release/throw and mouth-matrix attachment. Boomerang wind uses `item_awareness` to follow any active owner-local boomerang actor. This is the reusable flying-small-enemy pattern: vertical positioning stays selected-target state, retained wolf bite stays retained-owner state, and item wind/reaction stays item-awareness. |
| Shadow Keese | Shadow Keese | `src/d/actor/d_a_e_yk.cpp` | `E_YK` | policy-backed targeting, first-pass validated | Keese-family flyer batch pass. Wake/re-engage, approach flight, hover/orbit positions, dive startup, and battle attention height checks route through one Combat owner plus `selected_target_state`. Wolf-bite hold starts from `damage_owner`, then uses `wolf_catch_owner` for release/throw and mouth-matrix attachment. Boomerang wind uses `item_awareness`. No defender-owner, spawned-child, camera, or broader caught/grab surfaces were found in this pass. |
| Bubble | Bubble | `src/d/actor/d_a_e_bu.cpp` | `E_BU` | policy-backed targeting, first-pass validated | Flying-small-enemy batch pass. Wake/re-engage, approach flight, hover/orbit positions, dive startup, and battle attention height checks route through one Combat owner plus `selected_target_state`. Jump-cut cancellation after damage uses `damage_owner`; other damage tables and native head/knockback flow remain unchanged. No defender-owner, caught/grab, item-awareness, spawned-child, or camera surfaces were found in this pass. |
| Guay | Guay | `src/d/actor/d_a_e_ge.cpp` | `E_GE` | policy-backed targeting, first-pass validated | Circle-flight flyer pass. Wait/fly/caw/surprise awareness, attack orbit, dive gate, attack path obstruction, shield knockback angle, and selected-slot presentation-camera angle route through one Combat owner plus `selected_target_state`; boomerang wind uses `item_awareness` so any active owner's boomerang can trigger/focus the reaction. Arrow peer-surprise scans remain vanilla projectile proximity, and no defender-owner, caught/grab, spawned-child, or broader camera/demo surfaces were found in this pass. |
| Shell Blade | Shell Blade | `src/d/actor/d_a_e_sb.cpp` | `E_SB` | policy-backed targeting, pending validation | Armored-shell melee pass. The native cached Link position now comes from one Combat owner for search, jump facing, attack angle, and shield recoil; selected-target state supplies heavy-boots and status0 `0x4000` gates; sword/chance shell reactions use `damage_owner` cut facts from the player who struck it. Bomb/object searches remain object-owned behavior, not player targeting. No defender-owner, caught/grab, spawned-child, camera/demo, or broader collision-owner surfaces were found in this pass. |
| Ghost Rat | Ghost Rat | `src/d/actor/d_a_e_nz.cpp` | `E_NZ` | retained attach owner, switch-gate investigation | Focused retained-attach proof. Ceiling wake/drop fills the native player cache from the nearest active player in XZ, then lets vanilla `pl_check()` keep the original range/cone thresholds; this wake behavior is independent of wolf form. Testing in `D_MN10` room 10 showed the rats can be blocked before AI by native switch gates `227`-`229`. The switch-gate path now runs the same active-player wake predicate before action and raises the authored room switch when it passes, preserving the native group switch/state-machine flow instead of skipping the gate. `ghost_rat.state` records switch-gate and wake/drop facts; `world.switch` records the resulting switch source. Stick ownership uses `retained_interaction_owner` so body attachment, heavy-state counting, Midna rat-body routing, and joint matrix attachment follow the player the rat stuck to. Wolf-sense visibility only controls material/visibility and scans owner-local `selected_target_state` wolf-sense facts instead of P1 only. Native `stick_bit` remains a global occupancy bitmap; no camera/demo/spawn surfaces were found. |
| Peahat | Peahat | `src/d/actor/d_a_e_ph.cpp` | `E_PH` | retained hookshot carry owner + hookshot eligibility, pending validation | Focused hookshot carry/hang proof. Hookshot hits resolve through `damage_owner`, then Peahat retains a Carry owner through `retained_interaction_owner`; hang status bits, heavy-boots checks, and hookshot carry-offset writes follow that slot. Free-aim/attention eligibility now uses active-player awareness and slot-local hookshot status bits before a retained owner exists, so P2 should no longer be hidden by P1 distance/status. Ordinary authored stop-camera/demo paths remain P1/global presentation surfaces. |
| Shadow Beast | Shadow Beast | `src/d/actor/d_a_e_s1.cpp` | `E_S1` | wolf-bite owner plus roof/wake pass, pending validation | Confirmed in-game as the twilight Shadow Beasts, not the ordinary Skulltula. Focused wolf-bite ownership proof plus ceiling/drop wake pass. Damage that starts a wolf bite resolves through `damage_owner`, then `wolf_catch_owner` retains the biting wolf for release/throw and cleanup. ACT_ROOF drop distance now uses active-player XZ proximity, and switch-gated roof spawns can raise their authored wake switch from the same active-player predicate, mirroring the Ghost Rat switch-gate lesson. Shared `pl_check()` wake/continuation gates, close attack range, fight-facing, wolf bark/threat fear, and damage reaction facing route through co-op owner APIs. Shout/camera demo, group revival/demo presentation, and some damage-state reads remain deferred surfaces. |
| Stalfos | Stalfos | `src/d/actor/d_a_e_sf.cpp` | `E_SF` | policy-backed targeting + owner APIs, pending validation | Player-way checks, wake/LOS, action-wide XZ distance/angle/pitch cache, drawback facing, fight/attack gates, guard contact, and cut/head-jump reactions route through Combat target state, `defender_owner`, and `damage_owner`. Scripted intro/demo camera and player placement remain authored P1/global presentation until a dedicated demo/camera pass. |
| Poe | Poe / generic Poe | `src/d/actor/d_a_e_hp.cpp` | `E_HP` | policy-backed ghost targeting, pending validation | Wake/move/retreat/attack/down-state steering route through one Combat owner plus `selected_target_state`; wolf-form and wolf-sense reveal/vulnerability gates scan active players; ordinary hit diagnostics use `damage_owner`. Poe soul pull-out draw suppression and item-get/dead presentation remain P1/global or deferred soul/down-owner presentation surfaces. |
| Poe Variant | High-density Poe variant | `src/d/actor/d_a_e_po.cpp` | `E_PO` | advanced Poe combat/roll pass, first-pass validated | Action-wide distance cache, search height/facing, visible wait wake, wolf-sense visibility/attention gates, and advanced rolling/circling formation center/facing route through one Combat owner plus active-player wolf-sense checks. Damage-owner wolf-bite start and retained wolf-catch lifetime route through `damage_owner` plus `wolf_catch_owner`. Field testing found the advanced Poe encounter solid for P2 awareness. This is still not a full Poe completion: wolf-down pull, soul/item presentation, roll/holl demos, camera/player placement, special death flow, and authored opening/limbering sequences remain deferred down/soul-owner plus presentation surfaces. |
| Ghost Soldier | Ghost Soldier | `src/d/actor/d_a_e_gs.cpp` | `E_GS` | policy-backed proximity + wolf-sense visibility, pending validation | Appear/disappear proximity and facing metrics route through one Combat owner plus `selected_target_state`; wolf-sense visibility now scans active players instead of P1-only global wolf power. No damage-owner, defender-owner, caught/grab, spawn, or camera surfaces were found in this pass. |
| Rat | Rat | `src/d/actor/d_a_e_ms.cpp` | `E_MS` | policy-backed targeting + wolf-bite owner, pending validation | Full-distance wake/action cache, chase/attack gates, active wolf-bark awareness, room low-player trigger, damage-owner wolf-bite start, and retained wolf mouth/throw lifetime route through co-op APIs. Skull search/carry-object behavior remains object-owned native behavior. |
| Goose | Goose / Twilight Vermin | `src/d/actor/d_a_e_yg.cpp` | `E_YG` | policy-backed targeting + wolf-bite owner, pending validation | Full-distance wake/action cache, chase/swim/attack gates, active wolf-bark awareness, damage-owner wolf-bite start, and retained wolf mouth/throw lifetime route through co-op APIs. Skull/dokuro search remains object-owned native behavior; no demo/camera surface was found in this pass. |
| Bulblin Rider | Bulblin Rider | `src/d/actor/d_a_e_rd.cpp`, `src/d/actor/d_a_e_wb.cpp` | `E_RD`, `E_WB` | policy-backed targeting + mounted owner APIs, pending validation | Regular Link-target combat now fills Rider's native distance/angle dispatcher cache from one Combat owner while preserving authored coach/wagon targets. Body-contact awareness accepts any active player; club guard bounce uses `defender_owner`; regular hit reactions use `damage_owner` for head-jump, horseback hit, and jump-cancel cut checks. Ordinary mounted Rider run/attack gates use selected-target XZ distance, selected-slot horse ride/speed via `horse_owner`, selected-target boar ride for attack volume, and slot-local lock-on observation via `player_attention`; ordinary bow aim and spawned arrow launch math inherit the same Combat target while keeping native coach-arrow homing flags for actual coach/wagon targets. Ordinary rider-owned boar steering in `E_WB` now inherits the mounted Rider's retained Combat target for wait/launch, chase geometry, pass checks, and selected-slot horse speed, instead of owning a separate target or sampling P1. Twilit Carrier Kargarok's rider-carry demo retains the caught slot through the Rider and presents that slot with `event_presentation::EnemyRetainedInteraction`. Boar master/child setup, bridge/escort/King Bulblin-style setpieces, child/helper rider files, horn/death notifications, and setpiece demo cameras/player placement remain authored P1/global or dedicated future mounted/setpiece ownership surfaces. |
| Deku Like | Deku Like / Deku Flower | `src/d/actor/d_a_e_df.cpp` | `E_DF` | swallow owner + retained presentation proof, pending validation | Falling-player swallow awareness scans active players through `selected_target_state::status0_0x100`, and the swallowed ALINK is retained through eat, hold, spit-out, throw damage, miss cleanup, and delete via `retained_interaction_owner`. The Link-swallow camera/demo now presents the retained swallowed slot through `event_presentation::EnemyRetainedInteraction`, so P2 no longer gets moved to the eat/spit position while P1 watches the authored camera. Bomb/object eating remains object-owned native behavior. Quake/vibration routing, event acceptance, and non-Link object/bomb presentation remain authored P1/global or deferred surfaces. |
| Twilit Carrier Kargarok | Twilit Carrier Kargarok | `src/d/actor/d_a_e_yc.cpp` | `E_YC` | policy-backed targeting + wolf-bite/carry owner, pending validation | Flight/area, fast-fly, hover, attack, and action-wide angle/distance caches route through one Combat owner plus `selected_target_state`; damage-owner wolf-bite start and retained wolf-bite lifetime use `damage_owner` plus `wolf_catch_owner`. The catch attack starts a Rider-scoped `retained_interaction_owner::Carry` owner for the selected/caught player so Rider demo movement, throw damage, camera stop/reset, and presentation collapse consume the caught slot instead of P1. Rider/master linkage for authored spawn/setpiece presentation remains deferred. |
| Twilight Plant | Twilight Plant / Vine | `src/d/actor/d_a_e_yd.cpp` | `E_YD` | policy-backed targeting + bite owner, pending validation | Wake/home-distance, wait-look, attack, and action cache route through one Combat owner plus `selected_target_state`; shield bounce uses `defender_owner`; cut-state damage reactions use `damage_owner`; bite/caught lifetime uses `retained_interaction_owner` for the defender actually caught. Leaf helper spawning, vanish presentation, and broader object/collision ownership remain deferred. |
| Shadow Insect | Shadow Insect / Yami Mushi | `src/d/actor/d_a_e_ym.cpp`, `src/d/actor/d_a_obj_drop.cpp` | `E_YM`, `Obj_Drop` | surprise/fly combat + Tear pickup pass, first-pass validated | Action-wide target distance/angle cache, surprise-lock/near wake, surprise recovery, jump/fly attack strike-point producers, and fly/orbit attack continuation now route through one Combat owner plus `selected_target_state`, while reveal drawing and wolf-bark reactions scan active player state. Lock-triggered surprise samples the actual slot-local lock owner and preserves vanilla's distinction between ordinary lock-on and wolf lock, so P2 lock wake does not strand the insect in surprise movement. Tears of Light are collected by `Obj_Drop`, not the insect AI; the pickup object now retains the active player who enters the native collection radius through `retained_interaction_owner::Collect` so draw-in lines and `onWolfLightDropGet()` use that slot. Field testing confirmed the attack path and Tear pickup behavior. Dig/front-roll/river/rail/camera/Midna-sensitive paths, lock-cancel cleanup, and remaining retained/down interactions are still deferred Shadow Insect surfaces rather than combat targeting. |
| Lizalfos | Lizalfos | `src/d/actor/d_a_e_dn.cpp` | `E_DN` | humanoid combat owner pass, first-pass validated | Wake/LOS, action-wide dispatcher distance/yaw/pitch cache, player-way checks, wolf bark awareness, drawback facing, selected-slot hookshot-flight status, attack guard contact, regular damage-owner hit reactions, and retained wolf-bite lifetime route through Combat target state, `selected_target_state`, `player_camera_status`, `defender_owner`, `damage_owner`, and `wolf_catch_owner`. Hookshot shoot/return learn reactions, cut-bit/learn-defense globals, death/demo/talk notifications, and authored event/horse presentation paths remain deferred selected-target, item-awareness, or presentation surfaces. Field testing validated this as Lizalfos; Dynalfos is the separate `E_MF` actor. |
| Dynalfos | Dynalfos / armored Lizalfos variant | `src/d/actor/d_a_e_mf.cpp` | `E_MF` | humanoid combat owner pass, first-pass validated | Wake/LOS, action-wide dispatcher cache, player-way checks, wolf-bark awareness, selected-slot hookshot/iron-ball/spinner defense facts, attack guard contact, regular damage-owner reactions, item-awareness boomerang defense, and retained wolf-bite lifetime route through the same humanoid owner APIs as Lizalfos. Field testing found ordinary P2 combat behavior working. Event/talk notifications, authored horse/event presentation, and broader demo/death globals remain deferred. |
| Beamos | Beamos turret family | `src/d/actor/d_a_obj_bemos.cpp`, `src/d/actor/d_a_obj_lv6bemos.cpp`, `src/d/actor/d_a_obj_lv6bemos2.cpp` | `Obj_Bemos`, `Obj_Lv6bemos`, `Obj_Lv6bemos2` | turret target owner and authored wake producer, first-pass validated | Search/acquire, find-turn, beam length, beam pitch, and attack tracking route through one Combat owner plus `selected_target_state` while preserving native switch/object/death flow. Passive beam/model geometry consumes retained targets read-only instead of producing AI ownership before warning/search. `Obj_Lv6bemos2` wake acquisition filters active-player candidates through native range, vertical cone, facing cone, and LOS rules before choosing nearest. The dedicated `beamos.state` probe preserves action, mode, warning timer, activation/eye/body switches, whether the native wake check ran, and each active slot's first failed eligibility gate even when shared query buffers are crowded out. Room-4 switch attribution identified `d_a_swc00` (`SwAreaC`/`SwAreaS`) as the actual producer of activation switch 148. Its P1-only form/equipment gate and area hit test now evaluate each active player together, restoring the authored switch -> rise/warning -> native sight-check lifecycle for P2+ without bypassing the Beamos state machine. `Obj_Bemos` and `Obj_Lv6bemos2` were field-validated; the source-marked unused `Obj_Lv6bemos` variant was code-audited through the same target-owner policy. Push-pull keep/release, lock-on camera forcing, object damage/break behavior, and broader interaction ownership remain separate object/camera-owner surfaces. |
| Dark Kargarok | Dark Kargarok | `src/d/actor/d_a_e_yr.cpp` | `E_YR` | policy-backed targeting + defender owner, pending validation | View/wake, background line checks, attack movement, horse-aware special checks, and action-wide yaw/pitch/distance caches route through one Combat owner plus `selected_target_state`; attack guard bounce uses `defender_owner`. The selected-target horse-read still relies on the native global horse speed for the speed threshold, so slot-assigned horse-speed ownership is a deferred horse-owner surface if field testing exposes it. |
| Bombfish | Bombfish | `src/d/actor/d_a_e_bg.cpp` | `E_BG` | policy-backed water targeting, pending validation | Spawn wake, swim awareness, charge continuation, selected-slot heavy-boots/hook-carry gates, selected-slot camera-relative wait movement, and birth/recovery facing route through one Combat owner plus `selected_target_state` / `player_camera_status`. Fishing-rod bait/eat behavior, bomb/cargo state, hook carry object behavior, and broader water/object ownership remain native object-owned paths. |
| Skullfish | Skullfish | `src/d/actor/d_a_e_sg.cpp` | `E_SG` | policy-backed water targeting + retained bite, pending validation | Move/search/action cache, joint-attach approach, bite retention, selected-slot release input/status checks, and damage knockback owner route through Combat/awareness policy, `selected_target_state`, `player_camera_status`, and `damage_owner`. Fishing-rod bait search remains object-owned behavior; broader fishing/water ownership remains deferred. |
| Kargarok | Kargarok | `src/d/actor/d_a_e_kr.cpp` | `E_KR` | policy-backed targeting + horse/defender owner, pending validation | View/wake, auto awareness, attack steering, horse pursuit, action-wide distance/yaw/pitch cache, and attack guard bounce route through one Combat owner plus `selected_target_state`, `horse_owner`, and `defender_owner`. Coach/path/cargo-bomb behavior, the boomerang lock-cancel path, and other authored setpiece targets remain deferred item-awareness or mounted/setpiece ownership surfaces. |
| Bees / Beehive | Bees / bee swarm parent-child | `src/d/actor/d_a_e_bee.cpp`, `src/d/actor/d_a_e_nest.cpp` | `E_BEE`, `E_NEST` | parent hit-owner swarm pursuit, first-pass validated | Beehive hit-owner producers now store the damage/item owner in `mHitActorID` before child bees chase, so P2 hits do not seed a P1 swarm target. Bee follow state applies to the ALINK actor the swarm is actually chasing. Iron-ball and sword/boomerang-style bee knockback positions resolve through `damage_owner` so P2-owned hits do not borrow P1's item position/facing. Field testing found Bee and beehive swarm behavior solid. Camera-distance draw suppression, canoe/swim/event home timers, debris carry/look presentation, and broader swarm/spawner ownership remain deferred. |

## Reviewed Evidence

These rows have been inspected beyond the machine count. "Label evidence" is taken from strings/resource names/action names in the actor file; keep the original label when it is more honest than guessing an English name.

| Actor/File | Profile | Label Evidence | Classification | Targeting Notes |
| --- | --- | --- | --- | --- |
| `d_a_e_ai.cpp` | `E_AI` | Armos; HIO label `アモス`; actions wait/move/attack/damage/return | regular enemy candidate | Small action table; normal Armos batch target. |
| `d_a_e_ba.cpp` | `E_BA` | source header "Enemy - Keese"; arc variants `E_ba`, `E_fb`, `E_ib`; attack, wolf-bite, and wind actions | regular enemy candidate | Keese / Fire Keese / Ice Keese. Combat targeting/selected-target state, wolf-bite retained ownership, and boomerang item awareness are implemented pending validation. |
| `d_a_e_bi.cpp` | `E_BI` | enemy name `E_bi`; wait/up/move/water/disappear actions | regular/proximity candidate | Simple action table; likely useful for a low-risk wake/chase audit. |
| `d_a_e_bs.cpp` | `E_BS` | HIO label `ベビースタル`; normal/fight-run/attack/damage actions | regular melee candidate | Looks like a simple melee enemy shape with weapon model and guard/damage checks. |
| `d_a_e_bu.cpp` | `E_BU` | HIO label `バブル`; fly/fight/attack/chance/head actions | flying enemy candidate | Bubble. Combat targeting/selected-target state and damage-owner jump-cut cancellation are implemented and first-pass validated. |
| `d_a_e_cr.cpp` | `E_CR` | HIO label `クレイジーランナー`; move/damage actions | simple movement enemy candidate | Small file, likely good for movement/proximity conversion after identification. |
| `d_a_e_fk.cpp` | `E_FK` | HIO label `ファントム騎馬兵` | mounted/special enemy | Mounted/special; defer until ordinary enemies and mount-related targeting are understood. |
| `d_a_e_fz.cpp` | `E_FZ` | enemy name `E_fz`; wait/move/attack/damage actions | regular enemy candidate | Moderate action table; no HIO label found in first pass. |
| `d_a_e_gb.cpp` | `E_GB` | HIO label `デカババ`; head/flower actions, bomb-eat, drop-key demo | plant/special enemy | Giant Baba / Big Deku Baba. Head combat targeting and boomerang awareness are first-pass validated; flower/body, bomb-eat, key/drop, and demo presentation remain authored P1/global or object-owned behavior. |
| `d_a_e_gob.cpp` | `E_GOB` | HIO label `マグネゴロン`; fight/attack/defence/grab/jump/message actions | special/NPC-like combat actor | Despite enemy prefix, message/grab/special actions make it a poor first-wave target. |
| `d_a_e_hb.cpp` | `E_HB` | HIO label `デグババ（ボックリ）`; stay/appear/wait/attack/chance actions | plant enemy candidate | Deku Baba / Bokkuri Baba. Combat targeting, damage/defender ownership, and retained bite/caught ownership are first-pass validated. |
| `d_a_e_kk.cpp` | `E_KK` | HIO label `氷の剣士`; walk/spear throw/guard/attack/weaponmove actions | regular humanoid/ranged candidate | Chilfos / Ice Swordsman. First pass covers combat targeting, selected-target state, damage-owner cut/status reads, and parent-owned thrown spear launch ownership pending validation. |
| `d_a_e_pm.cpp` | `E_PM` | demo action and trumpet/glow models | demo/special enemy | Many demo and player demo calls; defer. |
| `d_a_e_sf.cpp` | `E_SF` | op-demo, guard, sitwait/crashwait/getup actions; HIO comment `スタルフォス` | regular humanoid with demo intro | Stalfos. Combat targeting, selected-target state, defender-owner guard contact, and damage-owner cut/head-jump reactions are implemented pending validation. First-contact demo camera/player placement remains authored P1/global presentation. |
| `d_a_e_st.cpp` | `E_ST` | HIO label `スタルチュラ`; search/shoot/jump/hang phases | multi-form regular enemy | Skulltula. First pass covers passive recognition, selected-target distance cache, local angle checks, hanging XZ drop/hold gates, hang-shoot height continuation, and small-damage facing through Combat/awareness policy. Grab/caught ownership, head-top hug positioning, and damage/cut reactions remain deferred. |
| `d_a_e_sw.cpp` | `E_SW` | chase/attack/bomb/hook/dive/catch actions | special mobile enemy | Hook/bomb/catch paths make it a later audit target. |
| `d_a_e_tt.cpp` | `E_TT` | HIO label `テクタイト`; wait/chase/attack/out-range actions | regular enemy candidate | Compact chase/attack state; promising first-wave candidate after audit. |

### Reviewed Batch: Swarms, Ghosts, Flyers, And Specials

These rows are from a second source pass. They are mostly classification gates: the point is to avoid accidentally choosing a boss helper, swarm, grab plant, or mounted/path actor as the first policy specimen.

| Actor/File | Profile | Label Evidence | Classification | Targeting Notes |
| --- | --- | --- | --- | --- |
| `d_a_e_bee.cpp` | `E_BEE` | bee sub-actions `ACT_HOME`, `ACT_FLY`, `ACT_FLY_HOME_A/B`; parent nest lookup | helper/swarm enemy | Tied to `E_NEST` and hit-actor state; classify with nest/spawner behavior, not standalone targeting. |
| `d_a_e_bg.cpp` | `E_BG` | swim/attack/bomb/birth/hook/eat actions; fishing rod bait lookup | water/special enemy | Bombfish. Spawn wake, swim awareness, charge continuation, selected-slot heavy-boots/hook-carry gates, selected-slot camera-relative wait movement, and birth/recovery facing are policy-backed pending validation. Fishing-rod bait/eat, bomb/cargo, hook carry object behavior, and broader water/object ownership remain deferred object-owned paths. |
| `d_a_e_bug.cpp` | `E_BUG` | group insect simple-model setup; boomerang and bomb searches | swarm/special enemy | Group behavior and item reactions make it a later swarm/hazard category. |
| `d_a_e_dd.cpp` | `E_DD` | search, attack, flame, bomb/tail/arrow damage actions | complex regular fire enemy | Dodongo. Combat targeting/selected-target state and damage-owner tail/jump-cancel reactions are implemented pending validation; bomb suction/search and bomb/arrow death counters remain native object/hit-type behavior. |
| `d_a_e_df.cpp` | `E_DF` | LinkEat/BombEat/ObjEat/Search/Wait actions | plant/grab special | Deku Like / Deku Flower. Falling-player swallow, retained ALINK eat/spit ownership, and retained-slot Link-swallow camera presentation are implemented pending validation; bomb/object eat remains object-owned, with quake/vibration and broader object/bomb presentation deferred. |
| `d_a_e_dk.cpp` | `E_DK` | wait/chase/attack/damage/death actions; separate core model | regular/complex enemy candidate | Bari. Search/chase/attack policy now routes through Combat plus selected target state; body/core split remains self-contained in the first pass. |
| `d_a_e_fb.cpp` | `E_FB` | wait/attack/damage/bullet actions; vertical player-distance gates | static/ranged special enemy | Big Freezard. Static/ranged combat targeting, selected-target vertical/head/LOS facts, selected-player heavy-boots/status0 gates, Mini Freezard spawn facing, active-player bullet hit counting, and active-player direct iron-ball hit counting are implemented pending validation. |
| `d_a_e_ge.cpp` | `E_GE` | wait/fly/attack/back/caw/wind/shield actions | flying enemy candidate | Guay. Combat targeting/selected-target state, selected-slot presentation-camera angle, and boomerang item awareness are implemented and first-pass validated. |
| `d_a_e_gi.cpp` | `E_GI` | sleep/wait and sword model evidence | regular humanoid/undead candidate | Gibdo. First pass covers combat targeting, ordinary damage-owner reads, and scream stun ownership; wolf-bite ownership remains deferred caught/grab work. |
| `d_a_e_gm.cpp` | `E_GM` | egg/core/wait/damage/rebound actions and statue checks | boss/miniboss special | Core/egg state and statue interactions make it a deferred special actor. |
| `d_a_e_gs.cpp` | `E_GS` | wait/appear/disappear alpha behavior | ghost/proximity special | Ghost Soldier. Proximity now routes through Combat plus selected target state; wolf-sense visibility remains a deferred selected-player/form-sense surface. |
| `d_a_e_hp.cpp` | `E_HP` | wait/move/retreat/attack/down/dead actions; HIO label `汎用ポゥ` | ghost enemy candidate | Poe / generic Poe. Combat targeting, selected-target steering, active wolf/wolf-sense gates, and damage-owner diagnostics are implemented pending validation. Poe soul pull-out draw suppression remains a deferred down/soul-owner presentation surface. |
| `d_a_e_hz.cpp` | `E_HZ` | hide/attack/away/wind/chance/water-death actions | complex regular/special enemy | Rich action table; defer until first-wave policy and damage ownership are stable. |
| `d_a_e_is.cpp` | `E_IS` | wait/move/attack/trap/poweroff/break/damage actions | regular/proximity candidate | Compact and promising for first-wave consideration if location/name are confirmed. |
| `d_a_e_kg.cpp` | `E_KG` | file header `Young Gohma`; move/attack/small-damage/damage actions; central `pl_check` gates | regular melee candidate | Young Gohma. Compact policy-friendly actor; first pass routes central targeting, LOS, and roof front-roll awareness through the co-op API families. |
| `d_a_e_kr.cpp` | `E_KR` | path/auto/attack/horse/wait/su-wait actions; coach and bomb references | mounted/path special enemy | Kargarok. Ordinary wake/auto/attack/horse/action-cache targeting and attack guard bounce are policy-backed pending validation. Coach/path/cargo-bomb behavior, boomerang lock cancel, and authored setpiece targets remain mounted/setpiece or item-awareness deferred surfaces. |
| `d_a_e_mb.cpp` | `E_MB` | parent boss lookup and first-demo actions | boss helper | Defer with boss/miniboss work. |
| `d_a_e_md.cpp` | `E_MD` | dummy/real action split, half-break/break/vibration, spear models | destructible/decoy special | More object-like than AI target selection; not part of first enemy policy wave. |
| `d_a_e_ms.cpp` | `E_MS` | file header "Rat"; skull search, move/attack/carry-object interactions | regular/helper hybrid | Rat. Combat targeting, active wolf-bark awareness, room low-player trigger, damage-owner wolf-bite start, and retained `wolf_catch_owner` mouth/throw lifetime are implemented pending validation. Skull/carry-object searching remains object-owned behavior. |
| `d_a_e_nz.cpp` | `E_NZ` | normal/attack/stick/damage actions | regular small enemy candidate | Ghost Rat. Wake/drop awareness now fills the native cache from the nearest active player in XZ before vanilla `pl_check()` runs; retained attach owner is implemented pending validation for body attach, heavy-state counting, Midna rat-body routing, and joint-matrix attachment. D_MN10 room 10 ceiling-drop rats are also gated by native switches `227`-`229`; the switch-gate path now raises the authored room switch when an active player satisfies the same local wake predicate, preserving group switch flow. Native `stick_bit` remains global occupancy. |
| `d_a_e_rb.cpp` | `E_RB` | appear/move/attack/disappear plus child coordination | spawner/child swarm enemy | Parent/child group behavior should wait until target policy can represent shared or inherited targets. |

### Reviewed Batch: Name Identification Pass

This batch resolves the "unclassified" rows from the machine inventory. Sources: genLabel strings, file header comments, achievement signal strings, resource names, and action tables. Confidence level noted per row.

| Actor/File | Profile | Label Evidence | Classification | Targeting Notes |
| --- | --- | --- | --- | --- |
| `d_a_e_fs.cpp` | `E_FS` | file comment "Enemy - Puppet"; appear/attack/move/damage actions | demo/special enemy | Possessed puppet enemy; demo paths present. Defer. |
| `d_a_e_ga.cpp` | `E_GA` | error string `蛾：シンプルモデル登録失敗しました`; model "E_Ga"; ga_fly/bt_fly | ambient/non-targeting | Simple floating moth swarm; no targeting logic needed. |
| `d_a_e_mm.cpp` | `E_MM` | internal HIO field `donketsu_*`; actions: normal/dash/defence/magne_wait/turn; `reflect_chance_time` | regular melee candidate | Shield-reflect enemy; two HIO size variants. Needs identity confirmation before first wave. |
| `d_a_e_oct_bg.cpp` | `E_OctBg` | class `daE_OctBg_c`; actions: born_swim/swim/chase_core/normal_attack | water/special enemy | Large aquatic predator; water policy needed first. |
| `d_a_e_ot.cpp` | `E_OT` | 20 egg spawn positions; animations: born/swim/damage; `dCcD_SE_SOFT_BODY` | swarm/spawner likely | Egg-hatching water creature; likely spawns from a parent actor. |
| `d_a_e_ph.cpp` | `E_PH` | file comment "Peahat Enemy"; appear/wait/fly/hang/damage | regular enemy candidate | Peahat. Hookshot carry/hang ownership is implemented pending validation through `damage_owner` plus `retained_interaction_owner`; free-aim/attention eligibility uses active-player awareness plus slot-local hookshot status bits before ownership is retained. Authored stop-camera/demo paths remain deferred P1/global presentation surfaces. |
| `d_a_e_s1.cpp` | `E_S1` | loads "E_S2" resource; `HAIR_STRAND_COUNT`/`HAIR_SEGMENT_COUNT` physics web; ACT_ROOF/ACT_WOLFBITE/ACT_SHOUT | regular enemy candidate | Shadow Beast. Wolf-bite ownership is implemented pending validation through `damage_owner` plus `wolf_catch_owner`; ACT_ROOF drop, switch-gated roof wake, first combat wake/fight gates, and bark/threat fear now use active-player/Combat owner helpers. Shout/camera demo and group revival/demo presentation remain deferred. |
| `d_a_e_sb.cpp` | `E_SB` | genLabel `シェルブレイド`; English comment "Shell Blade"; jump/attack/bomb-search/shield | regular melee candidate | Shell Blade. Combat targeting/selected-target state and damage-owner shell/chance reactions are implemented pending validation; bomb/object search remains object-owned behavior. |
| `d_a_e_sg.cpp` | `E_SG` | includes `d_a_mg_rod.h`; ACT_ESA_SEARCH (bait search); ACT_KAMU (bite); 19-joint segmented body | water/special enemy | Skullfish. Move/search/action cache, joint attach, retained bite release/status checks, and damage knockback owner are policy-backed pending validation. Fishing-rod bait search remains object-owned behavior; broader fishing/water ownership remains deferred. |
| `d_a_e_sh.cpp` | `E_SH` | genLabel `スタルハウンド`; English comment "Stalhound"; appear/move/stop/damage/dead | regular melee candidate | Stalhound (skeletal dog); compact state shape, promising for regular-wave audit. |
| `d_a_e_sm.cpp` | `E_SM` | `@brief Chu Worm`; HIO comment `スライム - Slime`; actions: normal/move/damage/attack/core_control | retained-interaction enemy candidate | Chu Worm / Slime. Focused pass covers combat targeting, defender contact, hookshot awareness, and retained hook carry ownership; field testing found P2 behavior matched expectations. Demo/camera presentation remains P1/global. |
| `d_a_e_sm2.cpp` | `E_SM2` | merge/split/roof actions; shared resource structure with E_SM | regular proximity candidate | Chuchu 2 / large splitting Chu. Normal move awareness, roof-drop proximity, and action target metrics now route through Combat target policy / active-player query; merge/split and water special states remain vanilla/self-owned. |
| `d_a_e_th.cpp` | `E_TH` | achievement signal `dark_hammer_one_hit`; includes `d_a_e_th_ball.h`; spiked chain ball tracking | special/miniboss-class enemy | Darkhammer (Palace of Twilight); chain-ball weapon is a separate linked actor. Defer. |
| `d_a_e_tk.cpp` | `E_TK` | genLabel `タドポール`; wait/find/attack/damage/swim/hide | regular enemy candidate | Tadpole. Wait/find/attack/path-swim policy now routes through Combat plus selected target state. |
| `d_a_e_tk2.cpp` | `E_TK2` | genLabel `タドポール` (larger variant); same action shape as E_TK | regular enemy candidate | Big Tadpole. Wait/find/attack policy now routes through Combat plus selected target state. |
| `d_a_e_vt.cpp` | `E_VT` | file comment "Variant Enemy (Death Sword)"; daE_VA_HIO_c; cloth/arm/finger/sword joints | miniboss/special enemy | Death Sword (Arbiter's Grounds mini-boss); many combat and demo states. Defer. |
| `d_a_e_wb.cpp` | `E_WB` | genLabel `イノシシ`; English comment "Wild Boar"; `LEADER_B_IKKI` / `LEADER_B_IKKI2` / `LEADER_B_LV9` defines; player-mount paths | mounted/boss/setpiece | Ordinary rider-owned boar steering inherits the Rider Combat target pending validation. King Bulblin's boar, player-mount paths, and multiple cavalry-battle setpiece states remain deferred. |
| `d_a_e_ws.cpp` | `E_WS` | genLabel `スタルウォーーーーーーーール`; wait/attack/move/climb; source comment says walltula | proximity/special enemy | Walltula. Wall/climb awareness and attack facing now route through Combat target policy plus selected-target state. |
| `d_a_e_ww.cpp` | `E_WW` | genLabel `ホワイトウルフォス`; wait/attack/chase/walk/run/damage | regular melee candidate | White Wolfos; chase/attack shape similar to E_OC. Good second-wave candidate. |
| `d_a_e_yc.cpp` | `E_YC` | file comment "Twilit Carrier Kargarok"; fly/rider-carry/wolfbite/damage | Twilight flying enemy | Twilit Carrier Kargarok. Combat targeting/flight facts, wolf-bite ownership, and Rider-scoped carry/presentation ownership are implemented pending validation; authored rider/master linkage and setpiece presentation remain deferred. |
| `d_a_e_yd.cpp` | `E_YD` | spawns `e_yd_leaf_class` sub-actor; `Z2SE_DARK_VANISH` on death; appears/moves/vanishes | Twilight enemy | Twilight plant/vine enemy with leaf sub-actor. Combat targeting, damage/defender ownership, and retained bite ownership are implemented pending validation; leaf helper and vanish presentation remain deferred. |
| `d_a_e_yg.cpp` | `E_YG` | file header "Twilight Vermin"; genLabel `グース`; normal/attack/swim/dokuro/damage/wolfbite | Twilight enemy | Goose / Twilight Vermin. Combat targeting, active wolf-bark awareness, damage-owner wolf-bite start, and retained `wolf_catch_owner` mouth/throw lifetime are implemented pending validation. Skull/dokuro behavior remains object-owned. |
| `d_a_e_yk.cpp` | `E_YK` | file header "Shadow Keese"; genLabel `闇キース`; wind/cruise/charge/damage/disappear | Twilight flying enemy | Shadow Keese; Twilight bat. Keese-family combat targeting/selected-target state, wolf-bite ownership, and boomerang item awareness are implemented and first-pass validated. |
| `d_a_e_yr.cpp` | `E_YR` | genLabel `闇カーゴロック`; English comment "Dark Kargarok" at line 2548; wait/hover/attack/fly/damage | Twilight flying enemy | Dark Kargarok. Combat targeting, selected-target flight facts, and defender-owner attack guard are implemented pending validation; selected-slot horse speed remains a deferred horse-owner surface. |
| `d_a_e_zh.cpp` | `E_ZH` | no HIO label; searches for `daObjCarry_c` lightball objects; `BCK_ZH_CATCH*/FLY_DELETE`; entrance model `BMDV_ZH_ENTRANCE` | boss-encounter special | Likely Palace of Twilight Zant's Hands (catches Sols/light orbs). Defer. |
| `d_a_e_zm.cpp` | `E_ZM` | HIO comment `ザントの首 Zant's Head`; tongue animations; search/move/attack/bullet | boss/story special | Zant's Head (from Zant boss encounter). Defer. |
| `d_a_e_zs.cpp` | `E_ZS` | includes `d_a_b_ds.h`; `daB_DS_c` boss lookup; killed by `AT_TYPE_SPINNER` | boss helper | Stallord encounter helper; Spinner-weapon dependent. Defer with Stallord boss work. |

## Callsite Classification Pass

This pass reads context around every player-singleton callsite in priority regular-enemy candidates to separate targeting, selected-target state, true primary/global state, damage ownership, and caught/grab ownership. The goal is not bespoke AI per enemy. The goal is to classify each callsite well enough that repeated enemy shapes can use one policy API without blindly redirecting story, damage-owner, or protagonist-specific state.

The central routing guide for these categories is `docs/coop-player-singleton-api-map.md`.

Boss files received a lighter pass. Treat boss files as protagonist-locked/deferred until a dedicated boss co-op audit proves otherwise.

### Classification criteria

**Targeting** - redirect to the co-op-selected target:
- Distance check that decides whether to search, chase, or start an attack against a player
- Angle check used to rotate toward or face a player
- Player pointer used to move toward, attack, or aim a projectile at a player

**Selected-target state** - should eventually read from the selected target, but requires explicit API support before conversion:
- Form checks that affect an enemy's reaction to the target, such as `checkNowWolf()`
- Target movement/combat state that affects pursuit or attack choice, such as speed, guarding, swimming, or damage wait
- Target pose/direction checks used as part of an attack decision

Do not leave these permanently primary-player-only just because V1 is cautious. Also do not redirect them blindly. They should move through `enemy_targeting` or a small target-state helper once the owning target is known.

**Primary/global state** - must remain unchanged unless a dedicated milestone says otherwise:
- Story-protagonist checks such as Midna presence, cutscene/demo participation, and scripted player placement
- P1-only HUD/camera/story state
- Equipment/combat checks that intentionally ask "what is the protagonist doing?" rather than "what is my selected target doing?"
- Demo/cutscene paths that lock to the story protagonist by design
- LOD/culling distance checks that use primary-player distance as an optimization proxy

**Damage-owner** - neither targeting nor primary/global state; belongs to a separate co-op damage-ownership pass:
- `dComIfGp_getPlayer(0)` fetched only to cast to `daPy_py_c*` inside a hit-reaction function, where the real question is "which player struck me?" not "which player should I target next?"
- Use `dusk::coop::damage_owner` for cut type/count, hit direction, weapon-owner, and hit-reaction ownership. Do not substitute nearest-player or current enemy target for attacker identity.

**Defender-owner** - enemy attack contact reads where the enemy is the attacker and the player is the defender:

- Guard/block checks after an enemy attack collider hits a player
- Reads such as "which player blocked this swing?" or "which player should receive guard reaction?"
- Use `dusk::coop::defender_owner`. Do not substitute `damage_owner`, nearest-player, current target, or P1 globals for defender identity.

**Caught/grab-owner** - neither targeting nor nearest-player policy; belongs to a separate caught-state ownership pass:
- Enemy is carrying, eating, restraining, hanging, or otherwise tracking a specific captured player
- Redirecting to nearest player is wrong after the grab begins; the actor must retain the captured owner until release

**Collision-owner** - no explicit search callsite; belongs to collision/world-interaction ownership:
- The enemy detects players through collision categories or contact callbacks rather than `fopAcM_searchPlayer*`
- These actors may not need `selectEnemyTarget()` at all, but they may still need owner-aware hit/contact handling

### Per-file findings

| File | Profile | Name | Targeting | Non-target state | Ambiguous | First-wave verdict |
| --- | --- | --- | ---: | ---: | ---: | --- |
| `d_a_e_tt.cpp` | `E_TT` | Tektite | 8 | 5 | 1 | **Safe** — chase/attack calls isolated in `executeChase`/`executeAttack`; state calls are cut-type and culling distance |
| `d_a_e_kg.cpp` | `E_KG` | Young Gohma | 3 | 2 | 0 | **Safe** — all distance/angle in `action()` helper; roof front-roll read is selected-target state; damage-check pointer fetch is a stub |
| `d_a_e_bs.cpp` | `E_BS` | Baby Stal | 5 | 3 | 0 | **Safe** — distance/angle clearly targeting; player cast in damage path is type-context only |
| `d_a_e_sh.cpp` | `E_SH` | Stalhound | 4 | 3 | 0 | **Safe** — targeting in move/attack; local P1 pointer reads at move/attack entry are target-position candidates, not story/demo state |
| `d_a_e_ai.cpp` | `E_AI` | Armos | 4 | 2 | 0 | **Safe** — angle/distance in `executeSearch`; directional check in `player_way_check` is primary-state |
| `d_a_e_fz.cpp` | `E_FZ` | Freezard | 5 | 3 | 0 | **Safe** — bounce/rotation targeting clear; `otherBgCheck` is line-of-sight to the target and should follow the selected target when this actor is converted |
| `d_a_e_dk.cpp` | `E_DK` | Bari | 2 | 3 | 0 | **Safe** — distance gates in `checkPlayerAttack`; home-distance and `otherBgCheck` are state |
| `d_a_e_sm.cpp` | `E_SM` | Chu Worm / Slime | 3 | 2 | 0 | **Focused pass first-pass validated** — action-wide XZ angle/distance, wake/LOS, defender contact, loose hookshot awareness, and retained hook carry are covered; demo/camera presentation remains P1/global |
| `d_a_e_bi.cpp` | `E_BI` | Bomb Bug | 2 | 3 | 0 | **Safe** — minimal distance search; relies mostly on collision detection |
| `d_a_e_mm.cpp` | `E_MM` | Donketsu | 2 | 2 | 0 | **Safe** — very low singleton use; minimal co-op targeting impact |
| `d_a_e_cr.cpp` | `E_CR` | Crazy Runner | 2 | 3 | 0 | **Safe** — no `fopAcM_searchPlayer*` pattern found; low co-op impact |
| `d_a_e_nz.cpp` | `E_NZ` | Ghost Rat | 0 | 2 | 0 | **Retained attach owner implemented pending validation** — low targeting count, but stick/body ownership is the important co-op path |
| `d_a_e_is.cpp` | `E_IS` | (unknown) | 0 | 4 | 0 | **Safe** — no targeting search; state pointer fetches only |
| `d_a_e_dd.cpp` | `E_DD` | (fire enemy) | 2 | 4 | 0 | **Safe** — two targeting calls; damage branch has cut-type state |
| `d_a_e_ba.cpp` | `E_BA` | Keese | 2 | 5 | 0 | **Converted first pass** — distance/angle and `checkSwimUp()` eligibility follow selected-target state; wolf-bite hold uses `wolf_catch_owner`; boomerang wind uses `item_awareness` |
| `d_a_e_ww.cpp` | `E_WW` | White Wolfos | ~30 | ~15 | 0 | **Second-wave** — 45 total callsites; wolf-form checks and demo logic mixed in; needs per-callsite read before any redirect |
| `d_a_e_sf.cpp` | `E_SF` | Stalfos | ~8 | ~12 | 0 | **Converted pending validation** — Combat targeting, selected-target cache, defender-owner guard contact, and damage-owner cut/head-jump reactions covered; story intro calls `changeOriginalDemo()`/`setPlayerPosAndAngle()` remain protagonist-locked presentation |
| `d_a_e_kk.cpp` | `E_KK` | Ice Swordsman | ~20 | ~16 | 0 | **Converted, first-pass validated** — combat targeting, selected-target damage/status facts, damage-owner cut/status reads, and thrown spear child launch ownership covered |
| `d_a_e_gi.cpp` | `E_GI` | Gibdo | ~10 | ~12 | 0 | **Current proof** — combat targeting, ordinary damage-owner reads, and scream stun ownership converted; wolf-bite ownership remains deferred caught/grab work |
| `d_a_e_hz.cpp` | `E_HZ` | (hazard enemy) | ~12 | ~7 | 0 | **Defer** — boots/armor/throw-damage checks are primary-player-specific equipment state |
| `d_a_e_st.cpp` | `E_ST` | Skulltula | ~15 | ~36 | 0 | **Partial first pass, pending validation** — passive recognition, selected-target distance cache, local angle checks, hanging XZ drop/hold gates, hang-shoot height continuation, and small-damage facing route through co-op policy; `getStCaught()` grab state, head-top hug positioning, and damage/cut reactions remain deferred until a caught/grab-owner pass |

### Boss files — protagonist lock status

All `d_a_b_*` boss files are treated as protagonist-locked/deferred for now. A targeted read of `d_a_b_yo.cpp` and `d_a_b_zant.cpp` confirmed that boss-shaped player lookups are embedded in story-fight state machines and should wait for a dedicated boss co-op audit:

- **d_a_b_yo.cpp** — mixed: angle/distance attack-pattern calls could technically be re-targeted, but position-capture logic is tightly coupled to P1 throughout. Protagonist-locked until boss co-op work begins.
- **d_a_b_zant.cpp** — many targeting-shaped calls (~18) embedded in story-fight state machines. Protagonist-locked/deferred until boss co-op work begins.

Remaining boss files (`d_a_b_bh`, `d_a_b_bq`, `d_a_b_dr`, `d_a_b_ds`, `d_a_b_gg`, `d_a_b_gm`, `d_a_b_go`, `d_a_b_oh`, `d_a_b_oh2`, `d_a_b_tn`) were not read in this pass. `d_a_b_gg` is Aeralfos/Gargoyle and stays in this boss/miniboss bucket, not the regular-enemy batch, until a dedicated boss co-op audit.

### Key findings from this pass

**Wolf-form and similar checks are selected-target state, not permanently primary-player state.** E_WW, E_GI, and likely others check `checkNowWolf()` to alter enemy reactions. V1 should avoid redirecting those calls blindly, but the long-term target is not "always ask P1". Once `enemy_targeting` has selected a target, these branches should use a target-state helper that asks the selected player's form/state when the branch is truly about that enemy's target.

**Grab/caught state needs ownership, not nearest-player targeting.** E_ST's `getStCaught()` checks are the clearest example: an enemy actively carrying a player must track that specific player, not whichever player happens to be nearest. Avoid grab-heavy files in the first policy wave unless state boundaries are proven clear. Later work should add a caught/grab owner model and convert only the grab paths that can retain a concrete captured player.

**Story intros and boss/setpiece scenes stay protagonist/global until a dedicated milestone.** E_SF calls `changeOriginalDemo()` and `setPlayerPosAndAngle()` during a scripted encounter intro. Those paths cannot be redirected by `enemy_targeting`. The same pattern likely appears in E_FS, E_PM, and any enemy with `op_demo`, `demo_wait`, or `changeDemoMode()` action states. Boss files are deferred/protagonist-locked for now, not because they can never support co-op targeting, but because boss cameras, phase scripts, and arena state need their own audit.

**`dComIfGp_getPlayer(0)` as a type-only cast is damage-owner work.** Several damage-check functions fetch the player pointer only to cast it to `daPy_py_c*` for a hit-reaction call, not to locate a target. These look like `daPy_py_c* pPy = (daPy_py_c*)dComIfGp_getPlayer(0); someHitCall(pPy->someField)`. If the hit reaction is owner-agnostic, the correct actor is the attacker, not P0. Flag these as **damage-owner callsites** rather than targeting or primary/global state.

**Some enemies have little or no ordinary `selectEnemyTarget()` surface.** E_IS and E_CR originally looked like this, and E_NZ proved the important warning: low targeting counts do not mean "easy." Ghost Rat needed retained attach ownership for stick/body effects rather than a broad targeting pass. Treat similar files as ownership-classification work first, not as automatic combat-targeting conversions.

**The desired conversion shape is repeated, not bespoke.** For regular enemies, the target should be owned once per actor behavior scope, then read through search/chase/attack/follow-through callsites that used to re-query P1. Per-file edits are still needed to hook each state machine, but the policy, retention, diagnostics, and target-state accessors should live in Dusk-owned reusable modules.

## Initial Actor Map

The table below is machine-assisted from `src/d/actor/d_a_e_*.cpp` and profile symbols. The lookup count is the number of direct matches for `fopAcM_searchPlayerDistance*`, `fopAcM_searchPlayerAngleY`, `dComIfGp_getPlayer(0)`, and `daPy_getPlayerActorClass()`. It is a triage signal, not a complexity score.

| Priority | Actor/File | Profile | Lookup Count | Initial Classification | Notes |
| --- | --- | --- | ---: | --- | --- |
| Done proof | `d_a_e_oc.cpp` | `E_OC` | 48 | regular melee | First regular-enemy proof; targeting, damage-owner, selected-target-state, defender-owner, selected-target obstacle steering, and active-player group participation paths are validated. |
| Done proof | `d_a_e_hm.cpp` | `E_HM` | low | proximity enemy | One wake trigger converted; full combat not audited. |
| High risk | `d_a_e_wb.cpp` | `E_WB` | 75 | mounted/boss/setpiece likely | Ordinary rider-owned boar steering inherits the Rider Combat target pending validation; King Bulblin/player-mount/setpiece paths remain deferred. |
| Converted, first-pass validated | `d_a_e_po.cpp` | `E_PO` | 63 | special/ghost-like | Generic Poe is handled in `E_HP`; ordinary action cache/search/wolf-sense wake, advanced rolling/circling formation, damage-owner wolf-bite start, and retained wolf-catch lifetime are converted. Dedicated ghost/down-owner work remains for wolf-down pull, soul/item presentation, roll/holl demos, camera/player placement, and special death flow. |
| High risk | `d_a_e_pz.cpp` | `E_PZ` | 60 | special enemy | High singleton density; classify before patching. |
| High risk | `d_a_e_rd.cpp` | `E_RD` | 56 | ranged/mounted/complex humanoid | Bulblin Rider. Regular combat target cache, ordinary mounted run/attack gates, bow aim/arrow launch, body-contact awareness, club guard, and basic damage-owner/horse-owner reads are implemented pending validation. Boar/coach/bridge/demo/camera and related rider helper files remain deferred setpiece/mounted ownership. |
| High risk | `d_a_e_vt.cpp` | `E_VT` | 50 | miniboss/special enemy | Confirmed: Death Sword (Arbiter's Grounds mini-boss). Many combat states and demo/camera calls; defer. |
| Current proof | `d_a_e_st.cpp` | `E_ST` | 48 | multi-form regular / grab-catch deferred | HIO label `スタルチュラ`; passive recognition, cached distance, local angle, and hanging XZ drop/hold gates are converted pending validation. `getStCaught()` grab-state, head-top hug positioning, and damage/cut reactions remain deferred until a co-op caught/grab-owner pass. |
| Current proof | `d_a_e_ww.cpp` | `E_WW` | 45 | regular melee candidate | Confirmed: ホワイトウルフォス (White Wolfos). Combat chase/attack/walk/move-out paths route through one Combat owner and selected-target state; master spawn staging can wake on P2, uses target-slot presentation angle, and hookshot side-step awareness scans active players. |
| Candidate | `d_a_e_ymb.cpp` | `E_YMB` | 42 | Twilit Bloat / Twilight Insect Boss | Header identifies this as `Twilight Insect Boss`; resource/sound names use `YB_*`, and lake/start/battle demo paths match the Lakebed Twilight boss presentation. High density with wolf-bite, wolf-lock, start/battle/lake demos, camera/player placement, and status gates; defer to retained-owner plus presentation/setpiece audit. |
| Candidate | `d_a_e_fm.cpp` | `E_FM` | 41 | special/grab/chain likely | Has demo/grab-heavy paths; defer until targeting policy exists. |
| Candidate | `d_a_e_rdy.cpp` | `E_RDY` | 36 | related humanoid | Likely related to `E_RD`; audit with that family. |
| Converted, first-pass validated | `d_a_e_kk.cpp` | `E_KK` | 36 | regular humanoid/ranged candidate | HIO label `氷の剣士` (Ice Swordsman); spear throw, guard, damage-owner, selected-target state, and parent-owned spear launch covered in first pass. |
| Converted, first-pass validated | `d_a_e_ym.cpp` | `E_YM` | 31 | Shadow Insect / Yami Mushi | Twilight Tear Shadow Insect family; action cache, reveal drawing, wolf-bark, surprise-lock/near, surprise recovery, attack strike-point producers, fly/orbit continuation, slot-local lock owner sampling, and `Obj_Drop` Tear pickup collection ownership are converted. Dig/front-roll, river/rail, camera visibility, lock-cancel cleanup, and Midna-sensitive paths remain deferred. |
| Candidate | `d_a_e_yh.cpp` | `E_YH` | 28 | grab/catch likely | Has caught-state style calls; likely needs player-state ownership audit. |
| Candidate | `d_a_e_db.cpp` | `E_DB` | 28 | grab/catch likely | Has caught-state style calls; likely needs player-state ownership audit. |
| Candidate | `d_a_e_sw.cpp` | `E_SW` | 27 | unknown regular/special | Needs name/location identification. |
| Converted, first-pass validated | `d_a_e_mf.cpp` | `E_MF` | 27 | Dynalfos / armored Lizalfos variant | Humanoid combat owner pass mirrors the validated Lizalfos shape: wake/LOS, action cache, wolf bark, selected hookshot/iron-ball/spinner facts, defender guard, damage-owner reactions, item-awareness boomerang defense, and wolf-catch lifetime are converted and field validated. Event/talk notifications, authored horse/event presentation, and broader demo/death globals remain deferred. |
| Candidate | `d_a_e_gob.cpp` | `E_GOB` | 25 | special/NPC-like combat actor | HIO label `マグネゴロン`; message/grab/special actions, not first wave. |
| Converted, first-pass validated | `d_a_e_dn.cpp` | `E_DN` | 25 | Lizalfos | Wake/LOS, action cache, wolf bark, selected hookshot-flight status, defender guard, damage-owner reactions, and wolf-catch lifetime are converted and field validated. Hookshot shoot/return learn reactions, cut-bit/learn-defense globals, death/demo/talk, and authored event/horse presentation remain deferred. Dynalfos is tracked separately as `E_MF`. |
| Candidate | `d_a_e_s1.cpp` | `E_S1` | 24 | regular enemy candidate | Shadow Beast; wolf-bite ownership, ACT_ROOF XZ proximity, switch-gated roof wake, first Combat wake/fight pass, and bark/threat fear are implemented pending validation, while shout/demo camera remain deferred. |
| Candidate | `d_a_e_dt.cpp` | `E_DT` | 24 | boss/setpiece likely | Many press/demo/special-position calls; defer. |
| Current proof | `d_a_e_gi.cpp` | `E_GI` | 22 | regular humanoid/undead candidate | Gibdo. Combat targeting, ordinary damage-owner reads, and scream stun ownership are policy-backed; wolf-bite ownership is deferred. |
| Done proof | `d_a_e_tt.cpp` | `E_TT` | 21 | regular enemy | HIO label `テクタイト`; compact chase/attack state; first non-Bokoblin port validated for targeting, damage-owner, and selected-target-state surfaces. |
| Candidate | `d_a_e_th.cpp` | `E_TH` | 20 | special/miniboss-class enemy | Confirmed: Darkhammer; `dark_hammer_one_hit` achievement signal; chain-ball weapon. Defer. |
| Candidate | `d_a_e_sf.cpp` | `E_SF` | 20 | regular humanoid with demo intro | Guard/sitwait/op-demo paths need care. |

## Reusable Conversion Groups

Use these groups to minimize manual per-enemy work. Each group should map to reusable `enemy_targeting` policy or helper APIs, while actor files supply only the local state-machine hook points.

| Group | Pattern | Reusable API Direction | Good Candidates | Deferred Hazards |
| --- | --- | --- | --- | --- |
| Ground search/chase/attack | Enemy wakes, turns, chases, and gates an attack by player distance/angle | Actor-local helper over `EnemyTargetScope::Combat`; callsite labels are diagnostics only | `E_OC`, `E_TT`, `E_KG`, `E_BS`, `E_SH`, `E_AI` | Demo intros, guard/damage-owner paths |
| Proximity/contact | Enemy reacts mostly through collision or a small wake radius | Collision-owner pass plus small query helpers where explicit search exists | `E_HM`, `E_BI`, `E_SM`, `E_SM2` | Hookshot/carry interactions, contact owner attribution |
| Vertical/flying/ranged | Enemy needs height, line-of-sight, projectile aim, or flight behavior | Later policy profile with vertical scoring and target-state helpers | `E_BU`, `E_GE`, `E_PH`, `E_YK`, `E_YR`, `E_FB` | Camera/story flyers, rider-carry paths |
| Target-state-sensitive | Enemy decision depends on target form/speed/guard/swim/damage/wolf-sense state | Use `dusk::coop::selected_target_state` after the target identity is known | `E_WW`, `E_GI`, `E_BA`, `E_NZ`; `E_KK` first pass covered | Accidentally reading P1 state for P2, or replacing protagonist-only state |
| Pathing/steering after target lock | Enemy already has a combat target, then samples player position/angle for detours, obstacle checks, or chase steering | Use selected-target state derived from the retained `enemy_targeting` result; do not run a fresh nearest-player query | `E_OC` first proof | P1-derived detour angles fighting a P2 combat target |
| Enemy-attack defender contact | Enemy attack collider hits a player, then code checks guard/block/defender state | Use `dusk::coop::defender_owner`; direct-player V1 proof surface is Bokoblin guard collision | `E_OC`, later humanoid melee enemies | Confusing defender identity with damage-owner or current target |
| Damage-owner | Enemy reaction depends on who hit it | Separate damage ownership API, later aggro/threat bias | Many humanoids and item-reactive enemies | Treating attacker identity as nearest target |
| Bespoke damage counters | Enemy bypasses ordinary HP and counts special hits or object thresholds manually | Actor-local helper over damage/collision owner facts; preserve native object exceptions | `E_FB` first proof; watch ice, armor, shell, and object-reactive enemies | P2 direct hits missing P1's bonus count; unsafe casts when non-object actors hit |
| Caught/grab/carry-owner | Enemy captures, carries, hangs from, or attaches to a specific player | Use the matching retained owner API: `caught_stun_owner`, `wolf_catch_owner`, `retained_interaction_owner`, or a future grab/swallow helper | `E_GI`, `E_BA`, `E_NZ`, `E_PH`, `E_S1`, `E_DF`; later `E_ST`, `E_SW` | Nearest-player retarget during a retained effect; confusing prompt `interaction_owner` with effect-lifetime ownership |
| Boss/setpiece/demo | Encounter state owns camera, script, phase, or protagonist placement | Dedicated boss co-op audit | `d_a_b_*`, `E_SF`, `E_FS`, `E_PM`, `E_VT` | Breaking story/camera/phase assumptions |

## First Policy-Backed Wave

The audit was sufficient to begin the `enemy_targeting` V1 module without broad enemy conversion.
Bokoblin now has the reusable policy spine plus damage-owner, selected-target-state, and
defender-owner proof surfaces. Tektite has been ported and validated as the first compact
non-Bokoblin specimen.

After Bokoblin, Tektite, Stalhound, Stalchild, Gibdo, Young Gohma, White Wolfos, Keese, and the current Chilfos first pass, the next choices are:

1. **Finish validating White Wolfos (`E_WW`)** - confirm P2 chase/attack, selected target speed/form behavior, and guard contact before broadening the pattern.
2. **Next target-state-sensitive enemy** - now reasonable because `selected_target_state` exists, but classify form/guard/damage reads before patching.
3. **A future caught/grab-owner proof enemy** - needed for wolf-bite hang ownership and similar retained physical interactions; Gibdo's wolf-bite path is explicitly deferred.
4. **Avoid grab-heavy or setpiece enemies** until caught/grab-owner and event/camera policies exist.

This sequence keeps the manual work small: build one policy API, convert one already validated actor, then port the same shape to one compact enemy before touching target-state-sensitive families.

## Audit Queue

Use this queue before writing more enemy behavior code:

1. ~~Finish label/resource extraction for unclassified actors.~~ Done — name identification pass complete. All previously blank rows in the machine inventory now have label evidence and a classification. A few remain uncertain (E_MM identity, E_SG exact species, E_ZH confirmation) but are classified well enough to defer safely.
2. Split the full inventory into buckets:
   - regular ground melee,
   - regular flying/ranged,
   - proximity/hazard,
   - helper/projectile/spawner,
   - plant/static special,
   - mounted/vehicle,
   - grab/caught-state,
   - miniboss/boss/story/demo.
3. ~~For each likely regular enemy, inspect only enough code to mark search/chase/attack/follow-through/damage risk. Do not patch during this pass.~~ Priority 1 regular-enemy callsite classification is complete enough for the first policy wave. Continue classification opportunistically for candidates outside that wave.
4. ~~Pick the first policy-backed wave from the best understood regular enemies, not necessarily from the highest lookup counts.~~ First wave chosen and validated: policy-backed `E_OC`, then `E_TT`. `E_SH` and `E_BS` passed first surface testing; `E_GI` is the current target-state-sensitive proof.
5. Keep target choice in `enemy_targeting`, selected target facts in `selected_target_state`, hit ownership in `damage_owner`, enemy-attack contact in `defender_owner`, and diagnostics quiet while selecting the next actor.

## Full Machine Inventory

This inventory is generated from `src/d/actor/d_a_e_*.cpp` file names and `g_profile_*` symbols. Lookup count uses the same four player-singleton patterns as the triage table. Classification is intentionally blank until reviewed against code/resource evidence.

| File | Profile | Lookup Count | Classification | Notes |
| --- | --- | ---: | --- | --- |
| `d_a_e_ai.cpp` | `E_AI` | 6 | regular enemy candidate | Armos; HIO label `アモス`; compact wait/move/attack/damage table |
| `d_a_e_arrow.cpp` | `E_ARROW` | 3 | helper/projectile likely |  |
| `d_a_e_ba.cpp` | `E_BA` | 10 | regular enemy candidate | Keese / Fire Keese / Ice Keese; combat target/selected-target state converted; wolf-bite hold and boomerang item awareness converted pending validation |
| `d_a_e_bee.cpp` | `E_BEE` | 5 | helper/swarm enemy | tied to `E_NEST`; bee follow, child chase seed, and hit-owner knockback positions now use contacted/damage-owner player facts; nest/home/swim/camera suppression remains deferred swarm/spawner ownership |
| `d_a_e_bg.cpp` | `E_BG` | 16 | water/special enemy | Bombfish; spawn wake, swim awareness, charge continuation, selected-slot heavy-boots/hook-carry/status gates, and birth/recovery facing converted pending validation; fishing rod bait/eat, bomb/cargo, hook carry object behavior, and broader water/object ownership deferred |
| `d_a_e_bi.cpp` | `E_BI` | 5 | regular/proximity candidate | compact wait/up/move/water/disappear action table |
| `d_a_e_bi_leaf.cpp` | `E_BI_LEAF` | 0 | helper/projectile likely |  |
| `d_a_e_bs.cpp` | `E_BS` | 8 | regular melee candidate | HIO label `ベビースタル` |
| `d_a_e_bu.cpp` | `E_BU` | 10 | flying enemy candidate | HIO label `バブル`; combat target/selected-target state converted; damage-owner jump-cut cancellation converted and first-pass validated |
| `d_a_e_bug.cpp` | `E_BUG` | 15 | swarm/special enemy | group insect/simple-model behavior; boomerang/bomb searches seen |
| `d_a_e_cr.cpp` | `E_CR` | 6 | simple movement enemy candidate | HIO label `クレイジーランナー` |
| `d_a_e_cr_egg.cpp` | `E_CR_EGG` | 0 | helper/projectile likely |  |
| `d_a_e_db.cpp` | `E_DB` | 32 | special/grab likely | caught-state style calls seen in scan |
| `d_a_e_db_leaf.cpp` | `E_DB_LEAF` | 0 | helper/projectile likely |  |
| `d_a_e_dd.cpp` | `E_DD` | 6 | complex regular fire enemy | Dodongo; combat targeting/selected-target state and damage-owner tail/jump-cancel reactions converted pending validation; bomb suction/search and bomb/arrow death counters remain native object/hit-type behavior |
| `d_a_e_df.cpp` | `E_DF` | 4 | plant/grab special | Deku Like / Deku Flower; falling-player swallow, retained ALINK eat/spit ownership, and retained-slot Link-swallow camera presentation implemented pending validation; object/bomb eating plus quake/vibration remain native/deferred |
| `d_a_e_dk.cpp` | `E_DK` | 8 | regular/complex enemy candidate | wait/chase/attack/damage/death; body/core split |
| `d_a_e_dn.cpp` | `E_DN` | 26 | complex humanoid enemy | Lizalfos; wake/LOS, action-wide cache, wolf bark, hookshot-flight status, defender guard, damage-owner reactions, and wolf-catch lifetime converted and field validated; hookshot shoot/return learn reactions, cut-bit/learn-defense globals, death/demo/talk, and authored event/horse presentation deferred |
| `d_a_e_dt.cpp` | `E_DT` | 24 | boss/setpiece likely | press/demo/special-position calls seen in scan |
| `d_a_e_fb.cpp` | `E_FB` | 21 | static/ranged special enemy | Big Freezard; static/ranged target/selected-target state converted; selected-player heavy-boots/status0 gates converted; Mini Freezard child spawn aim, active-player bullet hit counting, and active-player direct iron-ball hit counting converted pending validation |
| `d_a_e_fk.cpp` | `E_FK` | 9 | mounted/special enemy | HIO label `ファントム騎馬兵`; defer |
| `d_a_e_fm.cpp` | `E_FM` | 42 | special/grab likely | demo/grab-heavy calls seen in scan |
| `d_a_e_fs.cpp` | `E_FS` | 11 | demo/special enemy | file comment "Enemy - Puppet"; appear/attack/move/damage; demo/possessed enemy, defer |
| `d_a_e_fz.cpp` | `E_FZ` | 15 | regular enemy candidate | wait/move/attack/damage actions |
| `d_a_e_ga.cpp` | `E_GA` | 0 | ambient/non-targeting | 蛾 (Moth); simple floating swarm with no player targeting; ambient/environmental only |
| `d_a_e_gb.cpp` | `E_GB` | 17 | plant/special enemy | Giant Baba / Big Deku Baba; head combat targeting and boomerang awareness first-pass validated; bomb/key/demo presentation remains P1/global or object-owned |
| `d_a_e_ge.cpp` | `E_GE` | 9 | flying enemy candidate | Guay; circle-flight combat targeting/selected-target state converted; selected-slot presentation-camera angle and boomerang item awareness converted and first-pass validated |
| `d_a_e_gi.cpp` | `E_GI` | 28 | regular humanoid/undead candidate | sleep/wait and sword model evidence |
| `d_a_e_gm.cpp` | `E_GM` | 12 | boss/miniboss special | egg/core/rebound/statue checks |
| `d_a_e_gob.cpp` | `E_GOB` | 26 | special/NPC-like combat actor | HIO label `マグネゴロン`; message/grab/special actions |
| `d_a_e_gs.cpp` | `E_GS` | 2 | ghost/proximity special | appear/disappear proximity plus active-player wolf-sense visibility converted pending validation |
| `d_a_e_hb.cpp` | `E_HB` | 17 | plant enemy candidate | Deku Baba / Bokkuri Baba; combat targeting, damage/defender ownership, and retained bite ownership first-pass validated |
| `d_a_e_hb_leaf.cpp` | `E_HB_LEAF` | 0 | helper/projectile likely |  |
| `d_a_e_hm.cpp` | `E_HM` | 6 | proximity enemy | `e_hm.up_wait` proof converted |
| `d_a_e_hp.cpp` | `E_HP` | 12 | ghost enemy candidate | Poe / generic Poe; Combat targeting, active wolf/wolf-sense gates, and damage-owner diagnostics converted pending validation; soul pull-out draw suppression and item-get/dead presentation remain deferred P1/global or down/soul-owner surfaces |
| `d_a_e_hz.cpp` | `E_HZ` | 19 | complex regular/special enemy | hide/attack/away/wind/chance states |
| `d_a_e_hzelda.cpp` | `E_HZELDA` | 20 | boss/story likely |  |
| `d_a_e_is.cpp` | `E_IS` | 4 | regular/proximity candidate | compact wait/move/attack/trap/poweroff/break states |
| `d_a_e_kg.cpp` | `E_KG` | 5 | regular melee candidate | move/attack/damage around `pl_check` |
| `d_a_e_kk.cpp` | `E_KK` | 36 | converted, first-pass validated | HIO label `氷の剣士`; spear throw and guard; first pass covers parent-owned spear launch |
| `d_a_e_kr.cpp` | `E_KR` | 15 | mounted/path special enemy | Kargarok; ordinary view/wake, auto awareness, attack steering, horse pursuit, action cache, and attack guard bounce converted pending validation; coach/path/cargo-bomb behavior, boomerang lock cancel, and authored setpiece targets deferred |
| `d_a_e_mb.cpp` | `E_MB` | 1 | boss helper | boss monkey helper path |
| `d_a_e_md.cpp` | `E_MD` | 3 | destructible/decoy special | dummy/real half-break/break behavior |
| `d_a_e_mf.cpp` | `E_MF` | 28 | complex humanoid enemy | Dynalfos / armored Lizalfos variant; humanoid combat owner pass first-pass validated; event/talk/death and authored horse/event presentation remain deferred |
| `d_a_e_mk.cpp` | `E_MK` | 17 | miniboss/story special | boomerang monkey, demo-camera heavy |
| `d_a_e_mk_bo.cpp` | `E_MK_BO` | 10 | helper/projectile likely |  |
| `d_a_e_mm.cpp` | `E_MM` | 5 | regular melee candidate | internal codename `donketsu`; normal/dash/defence/magne_wait/turn/damage; shield-reflect mechanic; two size variants via HIO |
| `d_a_e_mm_mt.cpp` | `E_MM_MT` | 15 | helper/mounted variant | sub-actor or mounted variant for `E_MM`; audit with E_MM family |
| `d_a_e_ms.cpp` | `E_MS` | 7 | regular/helper hybrid | Rat; Combat targeting, active wolf-bark awareness, room low-player trigger, and wolf-bite retained ownership converted pending validation; skull/carry-object search remains object-owned |
| `d_a_e_nest.cpp` | `E_NEST` | 11 | spawner/nest likely | Beehive; hit-owner producer for `E_BEE` child swarm pursuit converted through `damage_owner`; debris carry/look, demo camera, boomerang debris search, swim/canoe/event home timers, and broader spawner ownership remain deferred |
| `d_a_e_nz.cpp` | `E_NZ` | 4 | regular small enemy candidate | Ghost Rat; retained attach owner implemented pending validation for stick/body effects, Midna rat-body routing, heavy-state counting, and joint attachment |
| `d_a_e_oc.cpp` | `E_OC` | 49 | regular melee | raw-query proof validated; policy-backed targeting V1 implemented for existing proof systems; selected-target obstacle steering and active-player group participation covered |
| `d_a_e_oct_bg.cpp` | `E_OctBg` | 8 | water/special enemy | large aquatic predator (Oct = Octorok-like, Bg = Big); born_swim/swim/chase_core/normal_attack; water enemy, defer until water-targeting policy exists |
| `d_a_e_ot.cpp` | `E_OT` | 8 | swarm/spawner likely | water egg-hatcher; born/swim/damage animations; 20 egg spawn positions; soft-body collision; likely spawns from a parent |
| `d_a_e_ph.cpp` | `E_PH` | 14 | regular enemy candidate | Peahat; file comment "Peahat Enemy"; hookshot carry/hang owner implemented pending validation; free-aim/attention eligibility now routes through active-player awareness and slot-local hookshot status; explicit camera/demo paths remain deferred |
| `d_a_e_pm.cpp` | `E_PM` | 18 | demo/special enemy | demo action and player demo calls |
| `d_a_e_po.cpp` | `E_PO` | 63 | special/ghost-like likely | High-density Poe variant; generic Poe is `E_HP`. Ordinary action cache/search/wolf-sense wake, advanced rolling/circling formation center/facing, damage-owner wolf-bite start, and retained wolf-catch lifetime converted and first-pass validated; dedicated down/soul-owner plus presentation pass still needed for wolf-down pull, soul/item presentation, roll/holl demos, camera/player placement, and special death flow |
| `d_a_e_pz.cpp` | `E_PZ` | 60 | special enemy likely | high singleton density |
| `d_a_e_rb.cpp` | `E_RB` | 9 | spawner/child swarm enemy | child coordination and parent tracking |
| `d_a_e_rd.cpp` | `E_RD` | 57 | ranged/mounted/complex humanoid | Bulblin Rider; regular combat target cache, ordinary mounted run/attack horse gates, bow aim/arrow launch, body-contact awareness, club guard, and basic damage-owner reads implemented pending validation; boar master/child setup, horn/demo/bridge/castle setpiece paths remain deferred |
| `d_a_e_rdb.cpp` | `E_RDB` | 18 | related humanoid/mounted likely | audit with `E_RD` family |
| `d_a_e_rdy.cpp` | `E_RDY` | 36 | related humanoid likely | audit with `E_RD` family |
| `d_a_e_s1.cpp` | `E_S1` | 25 | regular enemy candidate | Shadow Beast; wolf-bite owner, passive awareness diagnostics, ACT_ROOF XZ proximity, switch-gated roof wake, first Combat wake/fight pass, bark/threat fear, and damage reaction facing implemented pending validation; shout/demo camera remain deferred |
| `d_a_e_sb.cpp` | `E_SB` | 10 | regular melee candidate | シェルブレイド (Shell Blade); combat targeting/selected-target state and damage-owner shell/chance reactions converted pending validation; bomb/object search remains object-owned behavior |
| `d_a_e_sf.cpp` | `E_SF` | 20 | regular humanoid with demo intro | Stalfos; Combat targeting, selected-target cache, defender-owner guard contact, and damage-owner cut/head-jump reactions converted pending validation; op-demo camera/player placement remains P1/global |
| `d_a_e_sg.cpp` | `E_SG` | 8 | water/special enemy | Skullfish; move/search/action cache, joint attach, retained bite release/status checks, and damage knockback owner converted pending validation; fishing-bait search remains object-owned, broader fishing/water ownership deferred |
| `d_a_e_sh.cpp` | `E_SH` | 7 | regular melee candidate | スタルハウンド (Stalhound); appear/move/stop/damage/dead; skeletal dog; good audit candidate |
| `d_a_e_sm.cpp` | `E_SM` | 13 | retained-interaction enemy candidate | Chu Worm / スライム (Slime); combat targeting, defender contact, hookshot awareness, and retained hook carry first-pass validated; demo/camera presentation remains P1/global |
| `d_a_e_sm2.cpp` | `E_SM2` | 10 | regular proximity candidate | Chuchu 2 / large splitting Chu; merge/split/roof actions; normal move, roof-drop, and action metrics route through Combat target policy / active-player query |
| `d_a_e_st.cpp` | `E_ST` | 51 | multi-form regular enemy | Skulltula. Passive recognition, selected-target distance cache, local angle, hanging XZ drop/hold gates, hang-shoot height continuation, and small-damage facing converted pending validation; grab/caught, head-top hug, and damage/cut ownership deferred |
| `d_a_e_st_line.cpp` | `E_ST_LINE` | 0 | helper/projectile likely |  |
| `d_a_e_sw.cpp` | `E_SW` | 27 | special mobile enemy | hook/bomb/catch/dive actions |
| `d_a_e_th.cpp` | `E_TH` | 20 | special/miniboss-class enemy | Darkhammer; `dark_hammer_one_hit` achievement signal; carries spiked chain ball (E_TH_BALL); Palace of Twilight; defer |
| `d_a_e_th_ball.cpp` | `E_TH_BALL` | 2 | helper/projectile | Darkhammer's chain ball; audit with E_TH |
| `d_a_e_tk.cpp` | `E_TK` | 6 | regular enemy candidate | タドポール (Tadpole); wait/find/attack/damage/swim/hide; water-area regular enemy |
| `d_a_e_tk2.cpp` | `E_TK2` | 4 | regular enemy candidate | タドポール large variant; same action shape as E_TK; audit with E_TK |
| `d_a_e_tk_ball.cpp` | `E_TK_BALL` | 3 | helper/projectile | Tadpole projectile, including water and fire ball types; launch aim inherits parent Combat target and sword-reflection cut state uses `damage_owner` |
| `d_a_e_tt.cpp` | `E_TT` | 21 | regular enemy candidate | HIO label `テクタイト`; compact chase/attack state |
| `d_a_e_vt.cpp` | `E_VT` | 50 | miniboss/special enemy | Death Sword; file comment confirmed; daE_VA_HIO_c; cloth/arm/finger joints; mini-boss in Arbiter's Grounds; defer |
| `d_a_e_warpappear.cpp` | `E_WAP` | 11 | helper/effect likely |  |
| `d_a_e_wb.cpp` | `E_WB` | 75 | mounted/boss/setpiece | confirmed: イノシシ (Wild Boar); ordinary rider-owned boar steering inherits Rider Combat target pending validation; King Bulblin's mount, LEADER_B_IKKI variants/cavalry battle setpieces, and player-mount paths remain deferred |
| `d_a_e_ws.cpp` | `E_WS` | 2 | proximity/special enemy | スタルウォーーーーーーーール (Walltula/Stalwall); wall-crawling enemy; wait/attack/move/climb; Combat target plus selected-target state converted |
| `d_a_e_ww.cpp` | `E_WW` | 45 | regular melee candidate | ホワイトウルフォス (White Wolfos); HIO genLabel confirmed; wait/attack/chase/walk/run/damage; good second-wave candidate |
| `d_a_e_yc.cpp` | `E_YC` | 8 | Twilight flying enemy | Twilit Carrier Kargarok; combat flight targeting, wolf-bite owner, and Rider-scoped carry/presentation owner implemented pending validation; authored rider/master linkage and setpiece presentation deferred |
| `d_a_e_yd.cpp` | `E_YD` | 15 | Twilight enemy | Twilight plant/vine enemy; combat targeting, damage/defender ownership, and retained bite owner implemented pending validation; E_YD_LEAF/presentation deferred |
| `d_a_e_yd_leaf.cpp` | `E_YD_LEAF` | 0 | helper/projectile | E_YD leaf sub-actor; audit with E_YD |
| `d_a_e_yg.cpp` | `E_YG` | 8 | Twilight enemy | Goose / Twilight Vermin; combat targeting, active wolf-bark awareness, damage-owner wolf-bite start, and retained wolf-catch lifetime converted pending validation; dokuro/skull behavior remains object-owned |
| `d_a_e_yh.cpp` | `E_YH` | 29 | special/grab likely | caught-state style calls seen in scan |
| `d_a_e_yk.cpp` | `E_YK` | 10 | Twilight flying enemy | 闇キース (Shadow Keese); combat target/selected-target state converted; wolf-bite hold and boomerang item awareness converted and first-pass validated |
| `d_a_e_ym.cpp` | `E_YM` | 32 | Shadow Insect / Yami Mushi | Twilight Tear Shadow Insect family; action cache, reveal drawing, wolf-bark, surprise-lock/near, surprise recovery, attack strike-point producers, fly/orbit continuation, slot-local lock-owner sampling, and `Obj_Drop` Tear pickup collection ownership converted and first-pass validated; dig, front-roll, rail/river, camera visibility, lock-cancel cleanup, and Midna-sensitive paths remain deferred |
| `d_a_e_ym_tag.cpp` | `E_YM_TAG` | 0 | tag/helper likely |  |
| `d_a_e_ymb.cpp` | `E_YMB` | 42 | Twilit Bloat / Twilight Insect Boss | Header identifies this as `Twilight Insect Boss`; resource/sound names use `YB_*`, and lake/start/battle demo paths match the Lakebed Twilight boss presentation. High density with wolf-bite, wolf-lock, start/battle/lake demos, camera/player placement, and status gates; defer to retained-owner plus presentation/setpiece audit |
| `d_a_e_yr.cpp` | `E_YR` | 12 | Twilight flying enemy | 闇カーゴロック (Dark Kargarok); combat flight targeting and defender-owner attack guard implemented pending validation; selected-slot horse speed deferred to horse-owner if needed |
| `d_a_e_zh.cpp` | `E_ZH` | 2 | boss-encounter special | no HIO label; catches `daObjCarry_c` lightball objects (Sol/light orb); catch/fly/entrance mechanics; likely Palace of Twilight Zant's Hands; defer |
| `d_a_e_zm.cpp` | `E_ZM` | 9 | boss/story special | ザントの首 (Zant's Head); genLabel confirmed; flying boss head from Zant encounter; defer |
| `d_a_e_zs.cpp` | `E_ZS` | 7 | boss helper | Stallord encounter helper; references `daB_DS_c` (Boss Stallord); killed by Spinner (`AT_TYPE_SPINNER`); defer with Stallord |

## Next Steps

1. Choose the next regular enemy from an accessible test location, preferably a remaining target-state-sensitive second-wave enemy or another compact non-flying ground enemy.
2. Before patching, classify its singleton reads into targeting, selected-target state, damage-owner, defender/collision-owner, caught/grab-owner, primary/global, and render/culling.
3. Convert only the smallest coherent behavior slice, using actor-local helpers over the API families proven by Bokoblin and Tektite.
4. Continue classifying/test-locating remaining target-state-sensitive second-wave enemies in parallel.

## Multiplayer AI Notes

General multiplayer PvE practice supports the policy direction here: enemies should usually avoid instantaneous target flicker, preserve committed attacks, react to recent damage/threat, and distribute attention when many enemies are present. For this project, treat those as design heuristics until they are backed by local diagnostics and in-game behavior. Host-authoritative online play should make the host own enemy target decisions; clients can render or predict presentation, but should not independently decide enemy truth.
